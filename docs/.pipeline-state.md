# Pipeline State: Menu & Recipe System

## Status
Phase: test (complete)
Last updated: 2026-08-17

## Config
Project dir: /home/ishola/jar/compENG/sem-8/swe-project-menu-recipe-system (worktree)
Main repo: /home/ishola/jar/compENG/sem-8/swe-project
Docs dir: /home/ishola/jar/compENG/sem-8/swe-project-menu-recipe-system/docs
Feature branch: feature/menu-recipe-system (worktree, based on main @ 15f9b08)

## Artifacts
- Spec PRD: docs/prd-menu-recipe-system.md
- Spec TDD: docs/tdd-menu-recipe-system.md
- Task list: docs/tasks-menu-recipe-system.md (26 tasks, 4 phases, + 1 proactive BE-015)
- Implementation branch: feature/menu-recipe-system
- Implementation files (15, across 4 commits fc2a11b..21c08e1):
  prisma/schema.prisma, prisma/seed.ts, src/lib/recipe.ts,
  src/app/admin/menu/{page.tsx,MenuClient.tsx,actions.ts},
  src/app/admin/orders/{page.tsx,OrderClient.tsx,actions.ts},
  src/app/admin/orders/[id]/{page.tsx,OrderDetailsClient.tsx,actions.ts},
  src/components/layout/Sidebar.tsx, src/components/providers.tsx, AGENTS.md
- Test files (7, commit 77a06d6): vitest.config.mts, test/{setup.ts,mocks/next-cache.ts,helpers/fixtures.ts},
  src/lib/recipe.test.ts, src/app/admin/menu/{actions.test.ts,MenuClient.test.tsx},
  src/app/admin/orders/{actions.test.ts,OrderClient.test.tsx},
  src/app/admin/orders/[id]/{actions.test.ts,OrderDetailsClient.test.tsx}

## Environment Setup (done by orchestrator, do not redo)
- `npm install` and `npx prisma generate` already run in the worktree — node_modules is present.
- `.env` copied into the worktree from the main repo (it is gitignored, so the worktree had none).
- Local Supabase was NOT running (port 54322 refused, no containers). Orchestrator started it via
  `npm run supabase:start`. If `prisma db push` fails with a connection error, the stack is still
  coming up — wait and retry, do not switch databases.

## Phase History
### spec — 2026-08-17
Result: completed
Notes: Three new models — Dish, DishIngredient, OrderDish (snapshotted dishName/unitPrice).
OrderIngredientLog untouched => zero data migration, backward compatible. totalPrice is
derived-with-override. updateOrderIngredients replaced by unified updateOrderItems. Dish deletion
conditional: hard-delete if never ordered, soft-delete via isActive otherwise.

### plan — 2026-08-17
Result: completed
Notes: 26 tasks / 4 phases. Recipe + pricing math isolated into pure module src/lib/recipe.ts
(expandDishesToIngredients, computeDishSubtotal, mergeDuplicateIngredients) so the Vitest unit
layer can test it with no DB and no Next.js runtime. No hard blockers.

### impl — 2026-08-17
Result: completed (all 26 tasks; BE-001..014, FE-001..010, INFRA-001..002)
Notes: Verified task-by-task against the actual code on resume, not assumed. `npx tsc --noEmit`
exits 0. BE-012's endorsed deviation is implemented as planned (transaction returns
`{ order, ingredientTotals }`; the post-transaction low-stock loop iterates `ingredientTotals`,
not the removed `data.ingredients`). BE-013 held scope discipline — the diff is exactly the one
`tx.orderDish.deleteMany` call, no stock restoration, no try/catch, no auth. `updateOrderIngredients`
is fully removed from the codebase (grep-confirmed zero references).

### INFRA-002 verification pass — 2026-08-17 · 8/8 PASSED
Executed headlessly against the REAL Server Actions and the REAL local dev DB (freshly seeded
baseline: rice 80, tomatoes 25, chicken 40) rather than a re-implementation. Checklist items 1-5
plus the data half of item 7:

| # | Check | Result |
|---|---|---|
| 1 | `createDish` sums duplicate ingredient picks, no P2002 | PASS — 2 recipe rows, rice summed to 5 |
| 2 | 2 dishes sharing an ingredient -> ONE merged log row + correct decrement | PASS — rice qty 7, stock 73/23/39.5 |
| 2b | `OrderDish` snapshots dishName/unitPrice server-side | PASS — `2x Dish A@100`, `1x Dish B@50` |
| 3 | Editing an order twice nets stock correctly (revert-then-reapply) | PASS — chicken fully reverts 39.5 -> 40 |
| 3b | Dish-derived + manual extra line for same item merge to one row | PASS — qty 6 (2 dish + 4 extra) |
| 4 | Deleting an order with `OrderDish` rows, no P2003 | PASS — order gone, 0 leftover rows |
| 5 | Archiving a referenced dish preserves history, leaves picker | PASS — archived:true, snapshot intact, out of active picker, still in catalog |
| 5b | Never-ordered dish hard-deletes with its recipe | PASS — archived:false, dish + recipe rows gone |

Items 6 and 7 are UI-render checks (legacy-order empty state; archived dish still selectable in an
existing row's dropdown). Both are implemented and confirmed by code inspection at
`OrderDetailsClient.tsx:176` (empty state) and `optionsForRow()` at `:65-77` (synthetic
`(archived)` option with a catalog -> snapshot -> "Unknown dish" triple fallback). They were NOT
click-tested in a browser — they are handed to the Phase 4 component layer as required coverage,
which asserts them permanently rather than once.

Harness disposition: the checklist ran via a throwaway `.infra002/` directory at the worktree root
(its own header declared it never-committed and delete-after-run). It has been DELETED now that its
results are recorded here. Two findings from it carry forward into Phase 4 and must not be re-derived:
- `next/cache` must be stubbed to call a Server Action outside a Next request context, or
  `revalidatePath` throws "Invariant: static generation store missing". The DB work in every action
  happens before `revalidatePath`, so no-oping it exercises the real logic.
- `createOrder` fires REAL emails through Resend (pre-existing low-stock/notify path, live API key
  in `.env`). The integration layer MUST stub that path or every test run burns Resend quota.

### test — 2026-08-17
Result: completed
Notes: Bootstrapped Vitest (the repo's first test suite, permanent addition) with a two-project
config (`node` for unit/integration, `jsdom` + React Testing Library for components) — see
`vitest.config.mts`. `npm test` runs all layers. 66 tests / 7 files, all passing.
`npx tsc --noEmit` still exits 0.

Unit (`src/lib/recipe.test.ts`, 16 tests): `expandDishesToIngredients`, `computeDishSubtotal`,
`mergeDuplicateIngredients` — merge/skip/empty-input semantics, no DB or Next runtime.

Integration (real Server Actions against the real local Supabase Postgres instance, 25 tests
across `menu/actions.test.ts`, `orders/actions.test.ts`, `orders/[id]/actions.test.ts`): Dish CRUD
including the archive-vs-hard-delete branch; `createOrder`'s merged-log/snapshot/skip behavior;
`updateOrderItems` — including calling it twice in a row nets `currentStock` correctly (not
double-deducted, not under-reverted) and a dropped item reverts fully to baseline; `deleteOrder`'s
`OrderDish` cleanup for both dish-backed and legacy orders. Fixtures are scoped/randomized and
torn down per test (`test/helpers/fixtures.ts`) — global seed data was verified untouched after
the full run. `next/cache` is aliased to a no-op stub (`test/mocks/next-cache.ts`, wired via
`resolve.alias` in `vitest.config.mts`); `@/lib/notifications` is mocked in the `createOrder`
integration tests so no real Resend email is sent.

Component (React Testing Library over jsdom, 25 tests across `MenuClient.test.tsx`,
`OrderClient.test.tsx`, `OrderDetailsClient.test.tsx`): table rendering, create/edit dialogs,
recipe/dish-row add-remove, archive/restore/delete with differentiated outcomes, total-price
auto-recompute with manual-override behavior, and the two INFRA-002 items only verified by code
inspection before now — item 6 (legacy empty state) and item 7 (archived-dish-in-dropdown via
`optionsForRow()`), both now asserted permanently. `next/navigation`'s `useRouter` is mocked
(throws outside a mounted `AppRouterContext`, per Next 16's `next/navigation` source) and each
component's own `./actions` module is mocked (component layer, not integration).

**Finding — contradicts an AGENTS.md-documented landmine, needs a second look**: AGENTS.md
documents `<DialogTrigger render={<Button />}>` (used by `OrderClient.tsx`'s "Create Order"
trigger, among others) as silently broken — "click events swallowed." Under React Testing
Library + jsdom (a direct client-side render, no SSR/hydration step), clicking it reliably opened
the dialog with both `userEvent.click` and `fireEvent.click`, verified with an explicit
before/after DOM assertion. `OrderClient.test.tsx`'s docblock records this in detail. Rather than
silently trust or silently override either source, `OrderClient.test.tsx` tests the dialog's real
(open-and-working) behavior, and this note flags the discrepancy for the team: either the
AGENTS.md claim is stale, or the real bug is hydration-specific and doesn't reproduce in a
non-SSR component render — worth confirming in an actual browser before trusting either
document over the other going forward.

## Resolved Decisions
1. Worktree base — RESOLVED. Pre-existing WIP (17 files / 2273 insertions of dark-theme + branding
   + docs + seed work) committed to main as 15f9b08 BEFORE branching. docs/ deliberately excluded
   from that commit and committed onto the feature branch instead (d1ddc6e, a6507f0).
2. Test scope — RESOLVED. Vitest. Full layered suite (unit: recipe/pricing math; integration: Dish
   CRUD actions + dish-based createOrder/updateOrderItems; component: dish picker + catalog UI).
   Permanent repo addition — the repo's first test suite, not throwaway scaffolding.

## Verified Findings (carry into implementation)
- VERIFIED: all three existing admin clients (OrderClient.tsx:129, InventoryClient.tsx:186,
  CustomerClient.tsx:138) use `<DialogTrigger render={<Button />}>`, which AGENTS.md documents as
  silently broken. Their Create buttons are almost certainly dead today. Out of scope to fix, but
  new code MUST use `<Button onClick={() => setIsOpen(true)}>`.
- VERIFIED by planner: src/components/ui/toast.tsx exists but is imported by zero files and mounted
  nowhere. FE-003 mounts it globally. This is its first production use — its API is unexercised.
- deleteOrder MUST be extended to delete OrderDish rows or it throws a NEW crash for orders with
  dish line items. Required consequence of the schema change, not scope creep. Still must NOT
  restore stock.
- Planner deviation from TDD literal pseudocode (endorsed): TDD's createOrder low-stock loop
  iterates `data.ingredients`, a field that design removes. BE-012 returns `ingredientTotals` from
  the transaction and iterates that instead. Verify during review.

## Pre-existing bugs — OUT OF SCOPE (do not fix, do not worsen)
- Cancel/delete order does not restore inventory stock
- No authorization checks inside any Server Action
- Race-unsafe stock decrement (no floor/guard)
- No input validation (no zod in package.json)
