# Pipeline State: Quick-Win Polish Pack (Phase 1)

## Status
Phase: impl
Last updated: 2026-08-17

## Config
Project dir: /home/ishola/jar/compENG/sem-8/swe-project-polish-pack (worktree)
Main repo: /home/ishola/jar/compENG/sem-8/swe-project
Docs dir: /home/ishola/jar/compENG/sem-8/swe-project-polish-pack/docs
Feature branch: feature/polish-pack (from main @ 15f9b08)

## Scope (4 sub-features)
1. Mobile-responsive admin nav (hamburger/drawer below `md`)
2. Currency localization (Intl.NumberFormat + NEXT_PUBLIC_CURRENCY)
3. Due-date / overdue alerting (dashboard widget + row highlighting)
4. Brand assets + PWA wiring (icons/manifest metadata + rosty-logo)

## Out of scope (parallel pipelines)
- Server Action authz/validation/error handling, stock-revert-on-cancel (Phase 0, worktree swe-project-integrity-hardening)
- Menu/recipe/dish catalog (Phase 2, worktree swe-project-menu-recipe-system)
- The broken `<DialogTrigger render={<Button />}>` pattern (known separate issue)

## Artifacts
- Spec PRD: /home/ishola/jar/compENG/sem-8/swe-project-polish-pack/docs/prd-polish-pack.md
- Spec TDD: /home/ishola/jar/compENG/sem-8/swe-project-polish-pack/docs/tdd-polish-pack.md
- Task list: /home/ishola/jar/compENG/sem-8/swe-project-polish-pack/docs/tasks-polish-pack.md
  (30 tasks = 27 core + 3 proactive, across 4 phases; 9/10/7/1 per phase)
- Implementation branch: feature/polish-pack (created)
- Implementation files: pending
- Test files: pending

## Pre-survey findings (orchestrator, before spec)
- Price render sites (4, not the 2 named in the brief):
  - src/app/admin/orders/OrderClient.tsx:79 — table cell `` `$${info.getValue()}` ``
  - src/app/admin/orders/OrderClient.tsx:148 — form label "Total Price ($)"
  - src/app/admin/orders/[id]/OrderDetailsClient.tsx:72 — `${order.totalPrice}`
  - src/app/dashboard/page.tsx:102 — `${order.totalPrice.toFixed(2)}`
- src/components/ui/ contains: button, dialog, dropdown-menu, input, label, table, toast.
  NO sheet primitive exists — mobile drawer must build on dialog.tsx or a new primitive.
- src/app/admin/page.tsx has an existing `stats` array + `.stat-card` pattern the
  due-today/overdue widget should extend rather than replace.
- public/ brand assets ARE committed on main (favicon.ico, rosty-logo.jpeg,
  site.webmanifest, apple-touch-icon, android-chrome-{192,512}, favicon-{16,32}).
- Worktree had no node_modules; `npm install` run by orchestrator before impl phase.

## Phase History
### spec — 2026-08-17
Result: completed
Notes: Key decisions — drawer reuses existing Base UI dialog.tsx (no new sheet.tsx);
currency default NGN/en-NG (evidence: +234 phone codes + Nigerian names in prisma/seed.ts);
"today" pinned to Africa/Lagos via Intl.DateTimeFormat regardless of host TZ, date-granular,
injectable `now`; Vitest node-env unit tests only, manual QA checklist for nav/brand.
JUSTIFIED SCOPE ADDITION (orchestrator-verified): Order.dueDate was unsettable via any UI path
(createOrder accepts it, nothing ever passes it; seed doesn't set it either) — so the alerting
widget would read zero forever. A due-date input on order create + inline edit on order detail
were added as necessary work. Also created .env.example, which AGENTS.md lists but is genuinely
untracked in git.
Next.js 16 gotcha found by reading the docs: themeColor/colorScheme must be a separate `viewport`
export, NOT inside `metadata` (deprecated since 14).

### plan — 2026-08-17
Result: completed
Notes: Orchestrator was killed mid-phase by a Claude API session-limit error, but the planner had
already written tasks-polish-pack.md to disk. Resume run verified the artifact is complete and
coherent (1005 lines, dependency graph, per-task Definition of Done, env vars, open questions)
rather than re-running the planner. Planner independently cross-checked the TDD against the real
codebase and found 3 TDD defects, all with decisions already applied:
  1. TDD Rollout Plan claims globals.css is the only shared file; Sidebar.tsx and Header.tsx are
     each touched by BOTH the mobile-nav and brand/PWA items. Plan sequences those edits
     (FE-012/FE-013 before FE-014/FE-016) instead of treating them as parallel-safe.
  2. TDD Section 3 lists actions.ts as MODIFY, but createOrder already accepts+persists dueDate
     (dead-until-now code). No task created against that file; FE-006's DoD says so explicitly.
  3. TDD claims .env.example is net-new; it already exists in the worktree with the exact content
     including NEXT_PUBLIC_CURRENCY="NGN". INFRA-002 is a verify task, not a create task.
Two gating spikes were placed in Phase 1: VERIFY-001 (Base UI DialogClose click dispatch, gates
the drawer close button) and VERIFY-002 (prisma db push zero-diff, gates all due-date sub-tasks).

## Open Questions
Both are non-blocking with documented fallbacks (do not stall the pipeline):
1. Does Base UI `DialogClose`'s internal `render={<Button/>}` swallow clicks the same way
   `DialogTrigger render={<Button/>}` does (per AGENTS.md)? First-hour manual check during impl.
   Fallback already designed: `showCloseButton={false}` + explicit `onClick` handler.
2. Brand image assets are spelled "Rostty" (double-t) vs "Rosty" everywhere in app text.
   Content/business call for the owner, not engineering. Default: ship as-is.

## Known defect in the spec (carry forward to test-engineer)
tdd-polish-pack.md line ~516: the timezone-boundary unit test case contradicts itself
mid-sentence ("both instants are the same UTC calendar day (Aug 17 vs Aug 18 — actually
different UTC days already in this example...)"). The test-engineer must construct a correct
instant pair where the UTC calendar day differs from the Africa/Lagos calendar day
(Lagos is UTC+1, so the 23:00-24:00 UTC window is already the next day in Lagos).
