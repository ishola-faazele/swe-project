# Pipeline State: Menu & Recipe System

## Status
Phase: plan
Last updated: 2026-08-17

## Config
Project dir: /home/ishola/jar/compENG/sem-8/swe-project-menu-recipe-system (worktree)
Main repo: /home/ishola/jar/compENG/sem-8/swe-project
Docs dir: /home/ishola/jar/compENG/sem-8/swe-project-menu-recipe-system/docs
Feature branch: feature/menu-recipe-system (worktree, based on main @ 15f9b08)

## Artifacts
- Spec PRD: docs/prd-menu-recipe-system.md
- Spec TDD: docs/tdd-menu-recipe-system.md
- Task list: pending
- Implementation branch: feature/menu-recipe-system
- Implementation files: pending
- Test files: pending

## Phase History
### spec — 2026-08-17
Result: completed
Notes: PRD + TDD written. Design: three new models — Dish, DishIngredient, OrderDish
(with snapshotted dishName/unitPrice). OrderIngredientLog left untouched => zero data
migration, backward compatible. totalPrice is derived-with-override. updateOrderIngredients
is replaced by a single unified updateOrderItems action. Dish deletion is conditional:
hard-delete if never ordered, soft-delete via isActive otherwise.

### plan — 2026-08-17
Result: in progress

### impl — pending

### test — pending

## Resolved Decisions
1. Worktree base — RESOLVED. Pre-existing WIP (17 files / 2273 insertions of dark-theme +
   branding + docs + seed work) committed to main as 15f9b08 BEFORE branching, so the worktree
   contains the correct base for OrderClient.tsx and Sidebar.tsx. docs/ was deliberately
   excluded from that commit and instead committed onto the feature branch.
2. Test scope — RESOLVED. Vitest. Full layered suite (unit: recipe-expansion + pricing math;
   integration: Dish CRUD actions + dish-based createOrder/updateOrderItems; component: dish
   picker + catalog UI). Treat framework setup as a permanent repo addition, the repo's first
   test suite — not throwaway scaffolding.

## Verified Findings (carry into implementation)
- No test framework exists at all: no jest/vitest/playwright, zero test files. Phase 4 bootstraps
  from nothing.
- VERIFIED: all three existing admin clients (OrderClient.tsx:129, InventoryClient.tsx:186,
  CustomerClient.tsx:138) use `<DialogTrigger render={<Button />}>`, which AGENTS.md documents as
  silently broken. Their Create buttons are almost certainly dead today. Out of scope to fix, but
  new code MUST use `<Button onClick={() => setIsOpen(true)}>`.
- deleteOrder must be extended to delete OrderDish rows or it will throw a NEW crash for any order
  with dish line items. This is required, not scope creep. It still does not restore stock.
- Deleting an InventoryItem referenced by a DishIngredient hits the same unhandled-crash shape as
  the known deleteCustomer bug. Same pre-existing gap, new relation.

## Pre-existing bugs — OUT OF SCOPE (do not fix, do not worsen)
- Cancel/delete order does not restore inventory stock
- No authorization checks inside any Server Action
- Race-unsafe stock decrement (no floor/guard)
- No input validation (no zod in package.json)
