# Pipeline State: MinIO Object Storage + Direct-to-Bucket Image Uploads

## Status
Phase: COMPLETE (test phase finished directly by the orchestrating session, no subagents — the
project owner hit their weekly token limit and asked to finish without dispatching further agents)
Last updated: 2026-08-20 (test phase complete). Fixed all 47 tsc errors left by the implementation
phase (test-side fixture/mock churn from the schema-drop and rename, plus a full behavioral
rewrite of MenuClient.test.tsx for the table->card-grid change). Closed two real coverage gaps
the task list called out but no prior phase had reached: deleteDish's DishMedia cascade cleanup
(new integration case — this is the test that would have caught a missing
tx.dishMedia.deleteMany as an unhandled P2003), and updateProfilePhoto (new unit file addition +
new tests/integration/dashboard-actions.integration.test.ts, covering the "own row only"
authorization claim directly against the mock/real DB `where` clause, not just ok:true). Also
added real integration coverage for addDishMedia/removeDishMedia/reorderDishMedia (position
assignment, no-renumbering-on-remove, reorder swap, NOT_FOUND) alongside the mechanical fixture
cleanup. Fixed a stray lint regression (public/sw.js, a gitignored Serwist build artifact,
missing from eslint.config.mjs's ignore list — same bug class as 6cab9f5, same fix).

**Explicitly deferred, not done, due to the token-budget/speed constraint:**
TEST-007 (ProfilePhoto.test.tsx, new component test), TEST-009 (DishDetailsClient.test.tsx, new
component test), TEST-005's dedicated unit-level [id]/actions.test.ts (the same three actions
ARE covered by real integration tests added above — this is a coverage-shape gap, not a
behavioral one), and TEST-014 (manual QA: real video upload, iOS HEIC, full customer-facing
self-service flow). These are the four items worth picking up first in a future session before
this feature is considered fully test-complete by the original spec's bar.

**Full verification gate, all green:** tsc 0 errors, `npm test` 482 passed (28 files), `npm run
test:integration` 141 passed (17 files), `npm run lint` 4 pre-existing errors only (confirmed
untouched by this branch), `npm run build` succeeds with `/admin/menu/[id]` registered.

--- Prior state, superseded by the above, kept for history ---
Phase: impl (RE-RUN in progress — plan phase for the scope-change cascade completed, feature-developer dispatching now)
Last updated: 2026-08-20 (plan re-run complete) — plan phase of the scope-change cascade
(customer self-service photo, dish card gallery + `[id]` detail page, `DishMedia` one-to-many model)
is done. `implementation-planner` revised `docs/tasks-minio-image-uploads.md` IN PLACE: 24 tasks
(4 Infra/6 Backend/5 Frontend/8 Testing/1 Proactive) -> 34 tasks (4 Infra/9 Backend/7 Frontend/
14 Testing/2 Proactive). Full ID mapping (kept/renumbered/reversed/void/new) is in the task list's
own Summary section. No blocking open questions surfaced. Orchestrator verified ground truth
directly before dispatching the planner: `prisma/schema.prisma` still has `Dish.imageUrl` (not yet
dropped, no `DishMedia`/`MediaType` yet), `docker ps` confirms `chop-with-rostty-minio` still
healthy, `MenuClient.tsx` confirmed to have `RecipeBuilder`/`optionsForRow`/`buildIngredients`/
`recipePayload` inline (extraction target), `admin/menu/[id]/` confirmed not to exist yet,
`dashboard/actions.ts`'s existing 5 exports confirmed (for `updateProfilePhoto` placement),
`components/ui/image-upload.{tsx,test.tsx}` confirmed present under the old naming. Also checked
`git worktree list` across all 7 worktrees off this repo — only this worktree's (uncommitted)
`schema.prisma` has `Dish.imageUrl`/references it; no other worktree's committed schema conflicts,
but the shared-local-Postgres-across-worktrees convention means a `db push` from any other worktree
while this feature's column exists on the live DB could still drop it out from under this feature —
flagged to feature-developer as a pre-push coordination check, not a hard blocker.

--- Prior spec-revision status, superseded by the above, kept for history ---
Phase: spec (RE-RUN in progress — scope change, see Phase History's 2026-08-20 (later) entry)
Last updated: 2026-08-20 (later) — project owner requested a substantial scope change before
anything was committed: (1) customer photo moves from admin-managed to customer self-service on
the dashboard, changeable anytime; (2) admin dish table replaced with a card/grid gallery + a
dedicated `/admin/menu/[id]` detail page; (3) `Dish.imageUrl` scalar replaced with a one-to-many
`DishMedia` model supporting multiple images AND video (admin-only). Full cascade required per the
Backtracking rules: spec -> plan -> impl -> test all re-run. Revising `prd-minio-image-uploads.md`
and `tdd-minio-image-uploads.md` IN PLACE (not new files) since nothing is committed yet — this is
scope evolution of the same feature, not a new one. Orchestrator re-verified the repo directly
before touching anything: `git status` matches the prior COMPLETE-phase snapshot exactly (same
modified/untracked file list), `npx tsc --noEmit` = 0 errors, `npm test` = 469 passed / 28 files —
no drift since the prior session's verification gate. `docker ps` confirms
`chop-with-rostty-minio` still up and healthy.

--- Prior COMPLETE-phase status, superseded by the above, kept for history ---
Phase: COMPLETE
Last updated: 2026-08-20 (finished directly by the orchestrating session after test-engineer's
final dispatch stalled — it had already correctly written all remaining test files, including
`CustomerClient.test.tsx`, before the stall. Only 3 small, precisely-diagnosed items were left:
2 files with type errors purely from test-side type mismatches, not implementation bugs
(`image-upload.test.tsx` hand-rolled a looser local `UploadUrlResult` type instead of reusing
`ActionResult`; `client.test.ts` needed a `PutObjectCommand` cast for direct property access), and
3 failing `CustomerClient.test.tsx` assertions caused by a real (pre-existing-pattern, newly
surfaced) accessible-name gap in `CustomerClient.tsx`'s icon-only row buttons — `title="Delete
customer"` doesn't win the accessible-name computation over the sr-only span's shorter text, so
screen readers announced bare "Delete"/"Archive" on every row. Fixed by aligning the sr-only span
text to match the title text, which also improves real screen-reader UX, not just the test.

**Full verification gate, all green:**
- `npx tsc --noEmit`: 0 errors
- `npm test`: 469 passed / 28 files
- `npm run test:integration`: 141 passed / 16 files
- `npm run lint`: 4 errors, all pre-existing and confirmed untouched by this branch
  (`git diff --quiet main -- <path>`) — `src/app/~offline/page.tsx` and
  `src/components/layout/OfflineIndicator.tsx`, already known/flagged in an earlier phase
- `npm run build`: succeeds

Nothing committed yet — awaiting the project owner's go-ahead per this repo's "only commit when
asked" convention.

## Config
Project dir: /home/ishola/jar/compENG/sem-8/swe-project-image-uploads
Docs dir: /home/ishola/jar/compENG/sem-8/swe-project-image-uploads/docs
Feature branch: feature/minio-image-uploads (already created, branched from main@483c3ea)

Note on naming: `docs/.pipeline-state.md` (no suffix) already exists in this worktree, committed
on `main` — it belongs to a DIFFERENT, unrelated prior pipeline run (Phase 3 expansion:
notifications/phone-OTP/settings). Do not read it as this feature's state and never overwrite it.
This run's artifacts all use an explicit `-minio-image-uploads` suffix, matching the project's
established per-feature doc naming convention (see `prd-notifications.md`,
`prd-menu-recipe-system.md`, etc. in the same directory).

## Artifacts
- Spec PRD: docs/prd-minio-image-uploads.md (revised in place for the 3-part scope change)
- Spec TDD: docs/tdd-minio-image-uploads.md (revised in place for the 3-part scope change)
- Task list: docs/tasks-minio-image-uploads.md (21 -> 24 tasks for original scope; revised in place
  again for the scope-change cascade -> 34 tasks; see Phase History below)
- Implementation branch: feature/minio-image-uploads (already exists, still uncommitted)
- Implementation files (from the ORIGINAL scope, still sitting uncommitted in the working tree —
  feature-developer must revise/extend these, not start greenfield): .env.example, package.json,
  package-lock.json, prisma/schema.prisma, docker-compose.yml, src/lib/storage/ (client.ts,
  actions.ts), src/components/ui/image-upload.tsx, src/app/admin/menu/actions.ts,
  src/app/admin/menu/MenuClient.tsx, src/app/admin/customers/actions.ts,
  src/app/admin/customers/CustomerClient.tsx, src/lib/validation.ts. New files this revision's task
  list calls for: src/app/admin/menu/RecipeBuilder.tsx, src/app/admin/menu/[id]/{page.tsx,
  DishDetailsClient.tsx,actions.ts}, src/app/dashboard/ProfilePhoto.tsx, plus a rename of
  src/components/ui/image-upload.{tsx,test.tsx} -> media-upload.{tsx,test.tsx}.
- Test files: pending (test-engineer dispatches after feature-developer; Phase 4 TEST-* tasks
  excluded from feature-developer's scope, same split as the original impl phase)
- .env: copied file-to-file from sibling worktree swe-project/.env, MinIO vars already appended
  (INFRA-003 done) — no new vars needed for this revision per the TDD (video types are
  application-code-only, not infra config)

## Project Context
(Reuse this block verbatim in every subsequent phase's prompt, with the "don't re-discover"
instruction. Compiled by tpm-spec-writer from the orchestrator's pre-verification pass below plus
its own spec-writing pass.)

- **Stack**: Next.js 16 App Router + Turbopack, TypeScript strict (`@/*`→`src/*`), Prisma/Postgres
  via Supabase-CLI-managed local Docker (schema-push, no migrations), Supabase Auth, Tailwind v4 +
  Base UI/shadcn primitives (`class-variance-authority`, `cn()` via `tailwind-merge`), TanStack
  Table v8, Resend (email) + Arkesel (SMS) + WhatsApp Cloud API v24.0 for notifications. Zero
  `@aws-sdk/*`/S3/MinIO packages before this feature — genuinely greenfield.
- **Layering**: `page.tsx` (Server Component, Prisma fetch, `initialData` prop) → `*Client.tsx`
  (`"use client"`, `useState` only, no `useEffect` anywhere in the codebase) → `actions.ts`
  (`"use server"`, `revalidatePath`). Two Server Action conventions coexist: older bare-throw/
  no-validation (`menu/actions.ts`) vs. newer zod (`src/lib/validation.ts`) + try/catch +
  `ActionResult<T>` (`src/lib/errors.ts`'s `okResult`/`toErrorResult`/`ActionError`) — newer is the
  established target for all new work.
- **Key conventions**: `requireAdmin()` (`src/lib/auth.ts`) throws `AuthError`, first line of admin
  actions. `shortId` (not `id`) in user-facing strings. `isActive`-boolean archive pattern (never
  hard-delete a referenced row). Dialog "Create" buttons use direct `onClick={() => setIsOpen(true)}`,
  never `DialogTrigger render={<Button/>}`. TanStack Table `data` must be referentially stable
  (`useMemo` any derived/filtered array). `next/image` has ~zero usage in this repo (no
  `images.remotePatterns` configured) — this feature deliberately keeps using plain `<img>`.
- **Testing**: Vitest, `vitest.config.mts` (`node` for `*.test.ts`, `jsdom`+RTL for `*.test.tsx`) +
  separate `vitest.integration.config.mts` against isolated `rosty_integrity_test` Postgres, guarded
  by `tests/integration/guard-database-url.ts`. Never run `prisma db seed` in a test loop
  (destructive `deleteMany()`s). Schema changes are a human-gated, two-database `db push` procedure.
- **This feature's shape** (for planner/developer context, not to be re-derived): new
  `src/lib/storage/{client.ts,actions.ts}` module (mirrors `src/lib/notifications/` split — pure
  integration + thin `"use server"` wrapper); new `src/components/ui/image-upload.tsx` primitive;
  `Dish.imageUrl`/`User.imageUrl` nullable columns; `createDish`/`updateDish` migrated to
  `ActionResult` as a scoped rider (`deleteDish`/`toggleDishActive` deliberately left on the old
  pattern); new root `docker-compose.yml` (standalone from Supabase CLI's Docker lifecycle).

### Orchestrator pre-verification (verified directly, 2026-08-20, in this worktree)

### Orchestrator pre-verification (verified directly, 2026-08-20, in this worktree)
- Fresh worktree: no `node_modules` (npm install kicked off in background at dispatch time), no
  `.env` (gitignored, must be recreated per-worktree per project convention).
- `package.json` confirmed: **zero** `@aws-sdk/*`, `s3`, or `minio` packages present. Genuinely
  greenfield — no image/upload infra anywhere in `src/` or `prisma/schema.prisma` (confirmed by
  the user's own pre-dispatch grep, and spot-checked here).
- No `docker-compose.yml` at repo root. Local dev today is 100% `supabase start` (CLI-managed
  Docker, not compose-managed) — a new standalone compose file for MinIO is additive, not a
  refactor of anything.
- `prisma/schema.prisma`: `Dish` model (id/shortId/name/price/servingSize/isActive/timestamps,
  `ingredients`/`orderDishes` relations) and `User` model (id/shortId/name/email?/phone?/isActive/
  authEmail?/preferredLoginMethod/notify*/alert*/role/timestamps, `orders` relation) both confirmed
  as read. Both already use an `isActive` boolean archive pattern — no existing image/avatar field
  on either. Both use flat scalar columns, no JSON blobs — a new nullable image field should follow
  that same flat-column convention.
- `src/lib/auth.ts`: `requireAdmin()` is the established gate — throws `AuthError` (not an
  `ActionResult`), called as the first line of every mutating (and most reading) action. Confirmed
  present and stable; use it as-is for the new presigned-URL action, don't reinvent.
- **Real inconsistency found, spec should resolve it deliberately, not copy it blindly**: this repo
  has TWO coexisting Server Action conventions in currently-active files.
  - `src/app/admin/menu/actions.ts` (Dish CRUD) — OLDER pattern: `requireAdmin()` then bare
    Prisma calls, no zod validation, no `ActionResult` wrapping, throws propagate raw to the client.
  - `src/app/admin/customers/actions.ts` (Customer CRUD) — NEWER, established pattern:
    `requireAdmin()` + zod schema (`src/lib/validation.ts`) + try/catch + `ActionResult<T>` return
    (`okResult`/`toErrorResult`/`ActionError` from `src/lib/errors.ts`).
  - Prior project memory (tpm-spec-writer's own notes on this repo) explicitly records that new
    work should adopt the newer `ActionResult` contract even when touching an older-pattern file,
    not copy the older file's looser shape. The new presigned-URL action (and any Dish-image-field
    mutation) should use the newer pattern. Whether to also retrofit the *rest* of menu/actions.ts
    to the newer pattern is a scope call the spec should make explicitly (in-scope vs. explicitly
    deferred), not silently skip.
- `.env.example` convention confirmed: grouped by section with `# ── Section ──` headers, every var
  gets a multi-line comment explaining what reads it, what breaks if unset, and whether it's
  server-only vs safe-to-share-locally. New MinIO vars should match this documentation density, not
  just be bare `KEY=""` lines.
- `src/components/ui/` confirmed: `button`, `dialog`, `alert-dialog`, `dropdown-menu`, `input`,
  `label`, `switch`, `table`/`table-pagination`, `tabs`, `toast`, `skeleton`, `logo-loader`,
  `theme-toggle`, `highlight`. **No file-input or avatar/image-preview component exists yet** — the
  spec needs to design one net-new (following this project's Base UI/shadcn primitive style, not a
  new UI library).
- Both `MenuClient.tsx` (605 lines) and `CustomerClient.tsx` (515 lines) use the same create/edit
  dialog shape: `<form action={handleAdd}>` where `handleAdd(formData: FormData)` is a local async
  client function that reads `FormData`, then calls the real `"use server"` action. **This matters
  directly for the Instagram-flow requirement**: a presigned-URL direct-to-bucket upload sends file
  bytes straight to MinIO from the browser, never through this FormData/server-action round trip —
  so the uploaded object's key/URL must be tracked in a separate `useState` (mirroring the existing
  `newRecipe`/`editRecipe` state-holding pattern already used in `MenuClient.tsx`) and merged into
  the payload manually inside `handleAdd`/`handleEdit` before calling `createDish`/`createCustomer`,
  not submitted as a same-named form field. Design this explicitly in the TDD rather than leaving it
  to feature-developer to improvise.
- Both create-dialog "Add" buttons use direct `onClick={() => setIsOpen(true)}`, NOT
  `DialogTrigger render={<Button/>}` (which is a documented-broken pattern in this repo per
  AGENTS.md) — any new dialog/trigger the spec designs must follow the same direct-onClick pattern.

## Phase History
### spec (re-run) — 2026-08-20 (later)
Result: re-run, completed
Notes: PRD + TDD revised in place for the 3-part scope change (customer self-service photo, dish
card gallery + `[id]` detail page, `DishMedia` one-to-many model). No new blocking open questions —
the TDD's own carried-forward/new Open Questions sections were all non-blocking or already
resolved. Full cascade to plan/impl/test triggered per the Backtracking rules.

### plan (re-run) — 2026-08-20 (later)
Result: re-run, completed
Notes: `implementation-planner` revised `docs/tasks-minio-image-uploads.md` in place against the
revised TDD. 24 -> 34 tasks (4 Infra/9 Backend/7 Frontend/14 Testing/2 Proactive, up from 4/6/5/8/1).
Orchestrator pre-verified ground truth directly (schema.prisma, MenuClient.tsx, dashboard/actions.ts,
admin/menu/ and admin/orders/[id]/ directory listings, components/ui/, validation.ts, `git worktree
list` across all 7 sibling worktrees) and handed the planner a full old-task-list-vs-new-TDD cross
reference (superseded/void, reversed, carries-forward-unchanged, needs-revision, wholly-new buckets)
so it didn't have to rediscover the delta itself. Planner's own report confirmed: INFRA-001..004,
BE-002, TEST-001 marked `[DONE]`/`[UNCHANGED]`; BE-001/BE-005/BE-006/TEST-012(customer integration)
explicitly marked as REVERSED (not just revised) from the original scope's direction; old
FE-002/003/004 void, folded into a card-gallery rewrite + new dashboard component; FE-005 (old,
CustomerClient thumbnail column) renumbered to FE-007 and confirmed `[UNCHANGED]`, not redone.
Orchestrator spot-checked the revised file directly (964 lines, section headers/status tags grep'd)
rather than trusting the planner's report alone — matches the reported structure. No blocking open
questions surfaced (verified independently, not just accepted from the planner's claim). Planner
also proactively added PROACTIVE-001 (optional rate-limit suggestion on the widened customer upload
surface) beyond a literal TDD transcription — noted as optional, TDD explicitly accepts that risk.

### spec — 2026-08-20T13:30:00
Result: completed
Notes: tpm-spec-writer produced PRD + TDD. Orchestrator then personally verified the TDD's single
"blocking-for-planning" technical open question (MinIO CORS preflight behavior) with a real,
disposable MinIO container — see verification note under Open Questions below. Two genuine
product/scope questions remain and are blocking planning until the user answers them.

### plan — 2026-08-20T13:22:00 (task list generated), amended 2026-08-20T15:10:00
Result: completed, then amended (not a re-run — additive, no cascade)
Notes: implementation-planner produced docs/tasks-minio-image-uploads.md, 21 tasks across 4 phases.
The planner's own Open Questions section correctly surfaced "table-row thumbnail display" as a
genuinely open, non-blocking scope question (PRD user-story intent vs. TDD's dialogs-only Frontend
Changes scope) rather than silently deciding either way — flagged, not resolved, pending product
owner input.

2026-08-20T15:10:00 amendment: project owner resolved that open question — add small thumbnail
columns to both the MenuClient dish table and CustomerClient customer table, in this same pass,
not deferred. Orchestrator added FE-004 (MenuClient thumbnail column), FE-005 (CustomerClient
thumbnail column), and TEST-008 (their test coverage) directly to the task list, plus updated the
dependency graph, task counts, and Open Questions section in place. This is treated as a plan
amendment, not a plan re-run: it's a pure task addition inside the phase the planner already
scoped as "Integration & UI" for exactly this kind of work, doesn't touch or invalidate any
existing task (FE-001/002/003/BE-001/005/006 all unchanged), and per the orchestrator's own
"small corrections don't need the full cascade" rule this was handled directly (Read/Edit) rather
than re-dispatching implementation-planner. No PRD/TDD rewrite requested or needed — confirmed via
pre-verification that both `getDishes()`/`getCustomers()` already do unrestricted `include`
fetches, so `imageUrl` reaches the table with zero backend change beyond BE-001 (already planned).
Total: 21 -> 24 tasks (4 Infra, 6 Backend, 5 Frontend, 8 Testing, 1 Proactive).

### impl — dispatched 2026-08-20T15:15:00, confirmed complete 2026-08-20T16:00:00
Result: completed
Notes: Dispatched with the full amended task list (Phases 1-3, including FE-004/FE-005) as scope;
Phase 4 TEST-* tasks explicitly excluded (test-engineer's job next). Pre-verified before dispatch:
no aws-sdk in package.json, no imageUrl in schema.prisma, no MINIO vars in .env, no
docker-compose.yml at repo root, node_modules present (prior npm install completed), Docker 29.4.1
available and already running Supabase's own containers in this environment. Pre-declared expected
test churn: BE-005's ActionResult migration will break existing assertions in
tests/integration/menu-dish-actions.integration.test.ts — told feature-developer this is expected/
correct (TEST-005's job to fix, not feature-developer's, and not a reason to revert BE-005).
Standing reminder relayed verbatim: any MinIO/Docker setup difficulty must be stopped-and-reported,
not silently worked around.

**Prior run interruption note**: the session dispatching feature-developer hit an API/connection
error immediately before send. The orchestrator (this session, resumed) independently re-verified
feature-developer's actual completed work rather than trusting the interrupted report: ran
`npx tsc --noEmit -p tsconfig.json` fresh — **exactly 51 errors, confined to exactly 2 files**:
`tests/integration/menu-dish-actions.integration.test.ts` (47 errors, all `ActionResult` unwrap —
`.id`/`.name`/`.price` accessed on the union type directly) and
`src/app/admin/menu/MenuClient.test.tsx` (4 errors — 1 fixture-typing mismatch on `imageUrl`, 3
mock-return-shape mismatches missing the `ok`/`data` envelope). Zero errors anywhere else in the
project. `chop-with-rostty-minio` container confirmed healthy via `docker ps`. This matches the
prior session's own diagnosis exactly — Phases 1-3 (INFRA-001 through FE-005) are genuinely
code-complete and type-clean; the only remaining gap is these two pre-existing test files' fallout
from the `createDish`/`updateDish` → `ActionResult<Dish>` migration (BE-005), which is squarely
TEST-005's and MenuClient's test-coverage territory, not a feature-developer defect.

## Open Questions

### RESOLVED by orchestrator verification (no user input needed)
- **MinIO CORS preflight for the cross-origin presigned PUT** — TDD flagged this as
  "blocking-for-planning, needs live verification." Verified directly: spun up a disposable
  `minio/minio:latest` container (the exact image tag the TDD's `docker-compose.yml` specifies)
  with `MINIO_API_CORS_ALLOW_ORIGIN=http://127.0.0.1:3000`, created the bucket via `mc`, generated
  a real presigned PUT URL (boto3, SigV4, path-style — same signing shape `@aws-sdk/s3-request-presigner`
  produces), and confirmed end-to-end against the real running container:
  1. Browser-style OPTIONS preflight from the allowed origin returns proper
     `Access-Control-Allow-Origin`/`-Methods`/`-Headers` (204). A disallowed origin gets no CORS
     headers back (browser would block it) — confirms the allowlist is a real allowlist, not `*`.
  2. The real PUT with correct `Content-Type: image/jpeg` succeeds (200 OK).
  3. The same PUT with a *mismatched* `Content-Type: text/html` is rejected (403 Forbidden) — this
     is the live proof that `signableHeaders: new Set(['content-type'])` in the TDD's
     `createPresignedUploadUrl()` actually enforces content-type at the protocol level, not just in
     application code, backing the TDD's Security Considerations claim with real evidence rather
     than trusting the SDK docs.
  4. Anonymous GET on the object (matching `buildPublicUrl()`'s shape) succeeds with no auth,
     confirming the `mc anonymous set download` bucket policy the `minio-init` service applies is
     sufficient for permanent public display links.
  Test container torn down afterward (`docker stop minio-cors-test`, no `--rm` residue). The TDD's
  entire technical design — compose config, CORS config, presigning approach, content-type
  enforcement, public-URL approach — is now live-verified against the real image, not just
  documented as expected-to-work.

### RESOLVED — project owner answered, 2026-08-20
1. **`menu/actions.ts` mixed-convention scope**: CONFIRMED scoped as the TDD proposed. Only
   `createDish`/`updateDish` migrate to zod+`ActionResult`; `deleteDish`/`toggleDishActive` stay on
   the old bare-throw pattern as an explicit, deliberate deferral (not an oversight to re-flag).
   Verified against `docs/tdd-minio-image-uploads.md:228-247` — this is exactly what the TDD already
   specified as its own recommended default, so no PRD/TDD edit was needed, just confirmation.
2. **Customer photo access model**: CONFIRMED same public-GET model as dish photos — anonymous GET
   via non-guessable UUID key, no short-lived presigned GET URLs, consistent architecture across
   both image types. Verified against `docs/tdd-minio-image-uploads.md:584-588` — again the TDD's
   own recommended default, no doc edit needed.

Both answers matched the TDD's stated defaults on re-read; docs were not ambiguous on either point,
so `prd-minio-image-uploads.md` and `tdd-minio-image-uploads.md` are unchanged. Proceeding straight
to implementation-planner.

### RESOLVED — project owner answered, 2026-08-20 (plan-phase open question)
3. **Table-row thumbnail display** (surfaced by implementation-planner, not tpm-spec-writer):
   CONFIRMED in scope for this pass, not deferred. Add small thumbnail columns to both
   `MenuClient.tsx`'s dish table and `CustomerClient.tsx`'s customer table, using the same
   `imageUrl` field the create/edit dialogs already read/write. Captured as FE-004/FE-005/TEST-008
   in `docs/tasks-minio-image-uploads.md` (added directly by the orchestrator as a plan amendment,
   not a re-run — see Phase History above). No PRD/TDD edit needed.

### Non-blocking, flagged for awareness (deferred / informational)
- **iOS Safari / HEIC camera photos**: the content-type allowlist (jpeg/png/webp) excludes
  `image/heic`. Whether iOS Safari reliably re-encodes camera-sourced HEIC to JPEG before handing
  it to `<input type="file">` is common WebKit behavior but unverified on a real device here. Not
  blocking — flagged as a manual-QA item in the TDD's Testing Strategy, and will be called out again
  in the final pipeline summary.
- **Production deployment topology** (reverse proxy/CDN in front of MinIO): explicitly out of scope
  for this feature; `MINIO_PUBLIC_ENDPOINT` is a placeholder only.
